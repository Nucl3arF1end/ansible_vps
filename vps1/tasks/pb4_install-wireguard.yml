---

- name: Install Wireguard...
  apt:
    update_cache: yes
    name:
      - wireguard
      - wireguard-tools
      - qrencode
    state: latest

- name: Copy config and keys...
  copy:
    src: templates/wireguard/
    dest: /etc/wireguard
    follow: yes

- name: Restart wireguard daemon...
  systemd:
    state: restarted
    daemon_reload: yes
    name: wg-quick@wg0

- name: Enable wireguard daemon...
  systemd:
    state: enabled
    name: wg-quick@wg0
